version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p dist
        - python3 tools/generate_dashboard_config.py hosted-env --output dashboard/config.js
        - cp -r dashboard/* dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
